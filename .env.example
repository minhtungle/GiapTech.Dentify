# Copy to .env to override docker-compose defaults.
POSTGRES_DB=Dentify
POSTGRES_USER=dentify
POSTGRES_PASSWORD=dentify

# Public URL of the backend (AuthServer + API) and the frontend SPA.
SELF_URL=http://localhost:44348
CLIENT_URL=http://localhost:5173
